/**
 * selectSection.js
 * Written by: Anna Richardson
 */

/*
 * Action that is used to select component that is visible
 */

const selectSection = (section) => ({
  type: `SELECT_SECTION`,
  section,
});

export default selectSection;
